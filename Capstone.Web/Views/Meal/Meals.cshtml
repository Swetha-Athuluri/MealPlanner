@using Capstone.Web.Models
@model List<Meal>

@{
    ViewBag.Title = "Meals";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Meals</h2>

@if (TempData["action"] == "delete")
{
    <div class="alert alert-success" role="alert">
        <h5>Succesfully deleted meal!</h5>
    </div>

}
@*<div class="row">
    <div class="col-lg-6 col-lg-offset-1">

        <h1>Meals</h1>
    </div>

</div>*@



@{
    <div class="row">
        <div class="col-lg-offset-1">
            @for (int i = 0; i < Model.Count; i++)
            {

                <div class="col-lg-4">
                    <div class="row">
                        <div class="col-lg-12">
                            <h2>@Model[i].MealName</h2> 
                        </div>
                        
                    </div>
                    @for (int j = 0; j < Model[i].Recipes.Count; j++)
                    {
                        <div class="row">
                            <div class="col-lg-12">
                                @*<a href=""><img src="~/Recipe-Images/@(Model[i].Recipes[j].RecipeImageName)" class="img-circle" /> </a>*@

                                <p>@Model[i].Recipes[j].RecipeName</p>
                            </div>
                        </div>
                    }
                </div>
            }
        </div>
    </div>
}

            @*<div class="row recipeTile">
                    <div class="col-lg-4 recipeList">
                        <div class="col-lg-12">
                            <a href="/Recipe/Detail?RecipeId=@meal.MealId"><img src="~/Recipe-Images/@(meal.MealImageName)" class="img-circle" /> </a>
                            <div class="col-lg-12">
                                <a href="/Recipe/Detail?RecipeId=@meal.MealId"><h2 class="recipeText">@meal.MealName</h2></a>
                                <a href="/Recipe/Detail?RecipeId=@meal.MealId"><h2 class="recipeText">@meal.RecipeName></h2>
                            </div>
                        </div>
                    </div>
                </div>*@

            
